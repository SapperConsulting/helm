app:
  pod:
    port:
    healthCheckPath:
  service:
    name:
    protocol: TCP
    port:
  resources:
    requests:
      memory: "256Mi"
      cpu: "125m"
    limits:
      memory: "256Mi"
      cpu: "125m"
  secretName:
  distSecretName:
  annotations:
  is_client: false

image:
  name:
  tag:
  url:

metrics:
  enabled: true
  port: 8001
  path: /metrics
  interval: 30s

iam:
  service_account_role: ""
  service_account_name: ""
