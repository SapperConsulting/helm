app:
  pod:
    port:
    healthCheckPath:
  service:
    name:
    protocol: TCP
    port:
  resources:
    requests:
      memory: "256Mi"
      cpu: "125m"
    limits:
      memory: "256Mi"
      cpu: "125m"
  secretName:
  distSecretName:
  annotations:
  is_client: false

image:
  name:
  tag:
  url:

iam:
  service_account_role: ""
  service_account_name: ""

syncWave:
  deployment: 1

jobs:
  - name: db-migration
    syncWave: 0
    command: ["./migrate.sh"]
    resources:
      requests:
        memory: "256Mi"
        cpu: "100m"
      limits:
        memory: "512Mi"
        cpu: "200m"
